[Service]
# Preserve ProtectHome. systemd reads the root/user-owned capability and exposes
# a private, read-only copy only to this service.
LoadCredential=ai-broker.token:/home/joshan/.config/lifeos/ai-broker.token
Environment=LIFEOS_AI_BROKER_TOKEN_FILE=%d/ai-broker.token
