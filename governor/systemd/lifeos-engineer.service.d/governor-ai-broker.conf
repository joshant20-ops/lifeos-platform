[Service]
# Preserve ProtectHome. Expose only the Governor capability token needed for
# authenticated localhost API calls; no repository or model endpoint is visible.
BindReadOnlyPaths=/home/joshan/.config/lifeos/ai-broker.token
Environment=LIFEOS_AI_BROKER_TOKEN_FILE=/home/joshan/.config/lifeos/ai-broker.token
