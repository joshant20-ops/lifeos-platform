# Copy to /etc/lifeos/rundeck.env on Pi5, set mode 0600, and replace every value.
# Never commit the populated file.
LIFEOS_RUNDECK_BIND_IP=192.0.2.10
RUNDECK_ADMIN_PASSWORD=replace-with-a-long-random-secret
RUNDECK_DATABASE_PASSWORD=replace-with-a-distinct-long-random-secret
