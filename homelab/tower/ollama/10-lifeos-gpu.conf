[Service]
# LifeOS Tower local-AI policy: GPU availability is a machine/service property.
# Callers must not need per-request GPU flags.
# Pin Ollama to the Tower NVIDIA device; Ollama then automatically offloads
# model layers to CUDA whenever the model fits.
Environment="CUDA_VISIBLE_DEVICES=0"
