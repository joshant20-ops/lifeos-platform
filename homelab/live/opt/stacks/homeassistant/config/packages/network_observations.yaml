command_line:
  - sensor:
      name: network_observation_count
      unique_id: network_observation_count
      command: >-
        python3 -c "import json; from pathlib import Path; print(json.loads(Path('/config/network_observations.json').read_text())['count'])"
      scan_interval: 60
